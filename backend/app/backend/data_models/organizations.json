{
    "title": "organizations",
    "type": "object",
    "properties": {
        "id": {
            "type": "integer",
            "description": "Primary key, auto-increment"
        },
        "name": {
            "type": "string",
            "description": "Organization name"
        },
        "industry": {
            "type": "string",
            "description": "Industry type (e.g., Oil & Gas)"
        },
        "created_at": {
            "type": "string",
            "format": "date-time",
            "description": "Creation timestamp"
        }
    },
    "required": [
        "id",
        "name"
    ]
}